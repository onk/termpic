#!/usr/bin/ruby
require "optparse"
require "termpic"

options = {}
opt = OptionParser.new
opt.on('-f') {|v| options[:fit_terminal] = true }
opt.parse!(ARGV)
Termpic::Image.new(ARGV[0], options).draw

